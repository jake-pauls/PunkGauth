version "3.8"

services:
  # NGINX
  nginx:
    build: ./nginx
    container_name: nginx
    restart: always
    links:
      - server
      - react
    depends_on:
      - server
      - react
    ports:
      - "8080:80"
    volumes: 
      - ./nginx:/etc/nginx/conf.d

  # REACT
  react:
    container_name: react
    build: ./client
    restart: always
    volumes:
      - ./client:/usr/src
    expose: 
      - 3000

  # SERVER
  server:
    build: ./server
    container_name: server
    restart: always
    expose:
      - 5000
    volumes:
      - ./server:/usr/src